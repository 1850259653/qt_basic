# Snippets 索引

这里存放最小可复用片段，复制后即可嵌入你的页面。

- `ui_form_submit.py`：输入框 + 按钮 + 输出
- `data_sqlite_repo.py`：SQLite 建表与 CRUD 封装
- `data_http_get.py`：HTTP GET 请求与异常处理
- `thread_worker.py`：QThread + Signal 后台任务模板
